/**
 * Internal-only type for Framer Motion variants.
 * Kept separate from the public types/ folder because this is an
 * implementation detail of the animation helpers, not a domain type.
 */
import type { Variants } from "framer-motion";

export type FramerMotionVariants = Variants;
